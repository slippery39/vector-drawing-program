<template>
  <div id="q-app">
    <router-view />
  </div>
</template>

<script>
/* TODO: Flat structure for now.
var shapesState = {
  shapes: []
};
var shapesFunctions = {
  removeShape: function(id) {},
  addShape: function(shape) {
      //id should be auto assigned by the model.
    //check to make sure name is valid
      //name is valid if name is not blank.
    //other validity checks.
  }
  //Modify Shape?
};
*/

// functions for shapes?

export default {
  name: "App"
};
</script>
